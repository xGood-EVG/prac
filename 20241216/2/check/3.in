import asyncio
import random

async def main(A):
    tasks, B = await mtasks(A)
    print(len(tasks))
    random.shuffle(tasks)
    await asyncio.gather(*tasks)
    return B


random.seed(101201321)
A = random.choices(range(10000), k=21821)
B = asyncio.run(main(A))
print(*A)
print(*B)
print(B == sorted(A))